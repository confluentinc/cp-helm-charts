{{- if .Values.ssl.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cp-ksql-server.fullname" . }}-ssl-secret
  labels:
    app: {{ template "cp-ksql-server.name" . }}
    chart: {{ template "cp-ksql-server.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  kafka.client.truststore.jks: |-
{{- $path := printf "certs/%s" .Values.ssl.client.truststoreFile }}
{{ .Files.Get $path | b64enc | indent 4 }}
stringData:
  truststore_credentials: {{ .Values.ssl.client.truststorePassword }}
{{- end }}