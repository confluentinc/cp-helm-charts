{{- if .Values.secretConfOverrides }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cp-control-center.fullname" . }}-env
  labels:
    app: {{ template "cp-control-center.name" . }}
    chart: {{ template "cp-control-center.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretConfOverrides }}
  CONTROL_CENTER_{{ $key | replace "." "_" | upper }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
