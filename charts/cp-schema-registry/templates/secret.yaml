{{- if .Values.secretConfOverrides }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "cp-schema-registry.fullname" . }}-env
  labels:
    app: {{ template "cp-schema-registry.name" . }}
    chart: {{ template "cp-schema-registry.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  {{- range $key, $value := .Values.secretConfOverrides }}
  SCHEMA_REGISTRY_{{ $key | replace "." "_" | upper }}: {{ $value | b64enc }}
  {{- end }}
{{- end }}
